$("#posts_area").html("<%= j(render 'posts/index', {park: @post.comment.park, posts: @post.comment.posts, comment: @post.comment }) %>");
$("textarea").val('');
$("#notice").html("<%= j flash[:notice] %>");